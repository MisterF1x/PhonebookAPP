export const IconSize = Object.freeze({
  sm: '16px',
  md: '24px',
  lg: '32px',
});
export const routes = Object.freeze({
  HOME: '/',
  REGISTER: '/register',
  LOGIN: '/login',
  CONTACTS: '/contacts',
});
